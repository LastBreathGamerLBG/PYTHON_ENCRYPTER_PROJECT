from extractor import run

while True:
    text = str(input("Enter a Text: "))
    # here num defines how much data is encrypted by multipling it with its data sets 
    num = 40
    run(text, num)


    #you can also modify the script to use factorial 